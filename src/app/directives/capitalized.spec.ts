import { Capitalized } from './capitalized';

describe('Capitalized', () => {
  it('should create an instance', () => {
    const directive = new Capitalized();
    expect(directive).toBeTruthy();
  });
});
